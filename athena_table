SELECT
  from_iso8601_timestamp(eventTime) AS ts,
  awsRegion, eventSource, eventName,
  userIdentity.arn AS actor_arn
FROM ct_logs.cloudtrail_events
WHERE eventName LIKE '%Snapshot%'
  AND eventSource IN ('ec2.amazonaws.com','rds.amazonaws.com')
ORDER BY ts DESC
LIMIT 200;
