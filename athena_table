SELECT
  COALESCE(try(from_iso8601_timestamp(eventTime)), try(CAST(eventTime AS timestamp))) AS ts,
  awsRegion, eventName
FROM ct_logs.cloudtrail_ebs_json
WHERE eventSource = 'ec2.amazonaws.com'
  AND eventName LIKE '%Snapshot%'
ORDER BY ts DESC
LIMIT 100;
